version: '3'

services:

    ms.php82:
      container_name: "ms-php82"
      build:
        context: .
        dockerfile: ./Dockerfile
      ports:
        - '${APP_PORT:-80}:80'
      volumes:
        - '.:/var/www/html'
